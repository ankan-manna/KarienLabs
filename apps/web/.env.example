# Prompt 34 Part 13/17 — GA4 Measurement ID (e.g. G-XXXXXXXXXX). Optional:
# leave unset to disable Google Analytics entirely. Public, client-side
# config only — never put a secret/API key in a VITE_* variable, since
# everything with this prefix is bundled into the browser build.
VITE_GA_MEASUREMENT_ID=
