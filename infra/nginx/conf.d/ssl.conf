# Placeholder — nginx.conf's `include /etc/nginx/conf.d/*.conf` picks this
# file up even before SSL is provisioned, so the stack starts cleanly on a
# fresh checkout. Once you've run infra/scripts/init-letsencrypt.sh, replace
# this file's contents with infra/nginx/conf.d/ssl.conf.example (edited for
# your real domain) and reload nginx — see that file's header comment.
